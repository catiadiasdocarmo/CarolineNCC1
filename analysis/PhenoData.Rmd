---
title: "PhenoData"
author: "LucianoRogerio; Caroline Cardoso"
date: "2021-10-25"
output: workflowr::wflow_html
editor_options:
  chunk_output_type: console
---


## Ajuste dos dados fenotípicos para rodar a análise de modelos mistos

```{r Ajuste dos dados fenotipicos}
suppressWarnings(suppressMessages(library(tidyverse)))
library(reshape2); library(here)
DadosPhen <- read.csv2(here::here("data", "Dados fenotípicos BAG.CSV"), header = T, na.strings = "-")


colnames(DadosPhen)[c(8:34)] <- c("PCR","PTR","PPA","IC","AP","APsF",
                                  "NMa","DMCsg","HCNLab","DRY","PA",
                                  "PRNC","Vigor","RF","NR","PR","TAC","DMCoven",
                                  paste("HCN",1:9,sep=""))

DadosPar <- DadosPhen[,c(1:25)]

DadosParFin <- DadosPar %>% reshape2::melt(data = ., id.vars = c(1:7),
                                           variable.name = "Trait", value.name = "Value") %>%
  filter(!is.na(Value))

saveRDS(DadosParFin, file = here::here("output", "DadosParcelaCaroline.RDS"))

DadosHCN <- DadosPhen[,c(1:7,26:34)]

DadosHCNFin <- DadosHCN %>% reshape2::melt(data = ., id.vars = c(1:7),
                                           variable.name = "HCN",  value.name = "Value") %>%
  filter(!is.na(Value))

saveRDS(DadosHCNFin, file = here::here("output", "DadosHCNCaroline.RDS"))


```

### Atividades para proxima semana


-[ ] Criar uma coluna no arquivo csv com o tipo de delineamento do experimento, nome da coluna -> Delineamento
   DBC -> Delineamento em Blocos Casualizados
   DBA -> Delineamento em Blocos Aumentados
   
-[ ] Criar outra coluna no arquivo csv de nome controle
    Nos ensaios de delineamento em Blocos Aumentados e exigido a presenca de controles em todos os blocos. Os controles sao utilizados para relizar a predicao da variancia do residuo.
    0 -> clones que nao aparecem em todas os blocos
    1 -> Clones que aparecem em todos os blocos
    
-[ ] Identificar as parcelas com Blocos F e M para fazermos a devida correção    


Next - [Dados Moleculares - Seleção](GenoData.html)

[Home](index.html)
